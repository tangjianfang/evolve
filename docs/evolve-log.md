# evolve log — evolve (self-hosting experiment)

- verify: `python scripts/verify.py` (created round #1; pre-creation baseline = both `.claude-plugin/*.json` parse)
- push: auto-authorized (2026-09-05, for the first autonomous-run experiment)
- pointer: #37 (next round)
- rounds done: 36
- status: active — innovation run (opened 2026-09-05 by operator directive; run of 50 requested same day → rounds #23–#72, round #72 is the mandatory retrospective): all rounds target new/innovative capabilities, each round = research external sources (GitHub AI-skill repos, latest agent-tool mechanisms e.g. Claude Code, self-iteration frameworks) → adopt ONE mechanism that closes a real gap at low cost (E5: mechanisms, not architectures; record provenance) → verify (+checks = whitelist credential) → commit. Checkpoint due after #42. Previous run of 50 ended by operator instruction after #22 (v1.4.0 tag + local commits unpushed — operator decides; this run also does not push)
- checkpoint (after #32, run rounds #23–#32, 10/50): baseline 88 → 110 checks; v1.5.0 released locally (tag, unpushed); landed: mid-run lesson capture (#23), protocol-drift notice + check-update.sh (#24), seed lessons in template (#25), packaging lint (#26), mutation self-audit in replay audit (#27), driver round hook (#28), release (#29), untrusted-content red line + hostile-manifest fixture (#30), regressions & rollback procedure (#31), driver --dry-run (#32); lessons E12 added (#27, first live trigger of #23's mechanism). Pool: Tier R sources used — awesome-agent-skills/NVIDIA cuOpt, Claude Code changelog, Voyager/Reflexion/AlphaEvolve, Snyk ToxicSkills/CSA, Aider/Gemini CLI, Claude Code plan mode; next rounds need fresh sources (Codex CLI, OpenHands, DSPy, SRE postmortems, eval-driven dev). In-flight: none
- metrics: findings 75 | fixes 61 | regressions 0

## Target pool

(refreshed #15 — closed entries T1b/T1d/T1e/T1f/T1g, T4a/T4b/T4f, T3b/T3c/T3d dropped; their fix history lives in the round lines and CHANGELOG below)

- Tier R (innovation run, this run only — research-driven adoptions, one mechanism per round):
  - R1 scrape recent AI-skill repos / awesome lists on GitHub → adopt a high-rated self-iteration or skill-authoring mechanism
  - R2 latest agent-tool updates (Claude Code changelog: hooks, plugins, headless, memory…) → adopt an automation mechanism
  - R3 self-iteration frameworks (Voyager skill library, Reflexion, AlphaEvolve…) → adopt a mechanism that upgrades the lesson/crystallization loop
  - R4 open — reserve for the strongest remaining research lead from R1–R3

- Tier 1 (known defects):
  - T1a skill name collision: bare `evolve` resolves to `everything-claude-code:evolve` when that plugin is installed — mitigated (disambiguation note in both READMEs, re-verified #15); environmental, not closable from inside the repo
  - T1c install path never exercised: `/plugin marketplace add tangjianfang/evolve` + `/plugin install evolve@evolve-marketplace` untested in a clean environment — manual-only (needs a human in a clean env; do NOT test on a machine that already has a manual `~/.claude/skills/evolve/` install — it would register a duplicate skill source)
  - T1h (found #18, inspector pass-2) breaker.sh has no "pending epic decisions" stop channel — an all-parked autonomous run burns empty sessions until round N; the session-side termination rule exists (#18) but the driver never learns. Fix: session sets `status: pending-epics`, breaker stops on it (mirror of the converged channel) + fixtures + driver-header/README sync — closed #19
- Tier 2 (coverage gaps): empty (merged into T1b, closed #1)
- Tier 3 (module rotation — re-listed #15): SKILL.md, README.md, README.zh-CN.md, CHANGELOG.md, scripts/auto-evolve.sh, scripts/breaker.sh, scripts/verify.py, docs/templates/*, docs/lessons.md, .github/workflows/verify.yml — all rotated clean by #9–#15 (README install section was the last gap, fixed #15)
- Tier 4 (backlog — big-ticket, spec→plan flow, not round-sized):
  - trigger-match eval set: does the description actually win "iterate N times"-style prompts against competing skills
  - ≥20-round external long-run validation of the v1.1–v1.3 mechanisms on a real project
  - breaker residuals I2–I4 (#12) — deferred unless the evasion vector ever matters

## Rounds

#1 | T1b zero verification | findings(1) | actions(1) | result(green, 28 checks) | diff(~150) | scripts/verify.py — 28 structural checks (JSON, frontmatter, bilingual trigger, version consistency, links); profiling done in same commit; T1b closed
#2 | T4a no data-file templates | findings(1) | actions(1) | result(green, 31 checks) | diff(~60) | docs/templates/ (evolve-log / lessons / evolve-report) wired into SKILL.md profiling + READMEs; verify baseline 28→31; T4a closed
#3 | T4b no CI | findings(1) | actions(1) | result(green, 32 checks) | diff(~25) | .github/workflows/verify.yml runs the suite on push/PR; scope note: v1.0 spec excluded CI, superseded by the earn-back-10-points directive; baseline 31→32; T4b closed
#4 | T3a/b/c rotation + T1a | findings(4) | actions(2) | result(green, 32 checks) | diff(~20) | findings: README metrics example missing, T1a collision undocumented, CHANGELOG lags rounds 1–3, protocol gap (autonomous profiling) — first two fixed in both READMEs; latter two deferred to #5; T1a mitigated (registry precedence is environmental, documented not closed)
#5 | retrospective + T3c | findings(1) | actions(4) | result(green, 32 checks) | diff(~90) | lessons.md seeded E1–E4; SKILL.md amended (E3 autonomous verify, E4 counter-sum); release v1.2.0 (CHANGELOG + plugin/marketplace bump); summary block below
#6 | T4d ECC-benchmark borrow | findings(1) | actions(2) | result(green, 32 checks) | diff(~80) | comparative eval of everything-claude-code:evolve (different species: crystallizer vs engine); adopted its verified-counters + crystallization into retrospective (now 4 outputs); lessons template + own library gained verified column; E5 added; folded into unreleased 1.2.0
#7 | T4e autonomy + anti-gaming | findings(1) | actions(4) | result(green, 33 checks) | diff(~170) | user directive: full automation, no fake progress — added Autonomous mode (one round per headless session, circuit breaker, conditional auto-push) + Anti-gaming rules (progress whitelist, adversarial inspector, novelty/difficulty guards, verification lock, replay audit) + scripts/auto-evolve.sh driver; release v1.3.0; NOTE: autonomous mode itself not yet validated by a real headless run
#8 | pool-refresh + T3 rotation (SKILL.md, auto-evolve.sh, verify.py) | findings(3) | actions(1) | result(blocked, 33 checks NOT executable — permission denial) | diff(~45) | first real autonomous/headless round (validates #7's mechanism end-to-end incl. resume-from-pointer); pool refresh triggered (full sweep done): T1d SKILL.md retrospective duplicate list item, T1e auto-evolve.sh circuit-breaker tail-window bug, T1f allow-list tool-prefix gap (`Bash(python:*)` ≠ PowerShell session → verify unrunnable); NO code fixes committed — red line "tests green before commit" cannot be satisfied without executing verify, and hand-simulating checks would be narrative progress; all three fixes pre-staged as Tier 1 entries for #9
#9 | T1f gate fix + T1d dedup | findings(1) | actions(2) | result(green+progress, 34 checks) | diff(~40) | T1f: `PowerShell(...)` rules live in settings.local.json (local, untracked) + tool-prefix requirement documented in auto-evolve.sh header + both READMEs; verify denial reproduced RED (twice: #8 and #9-open), then executable GREEN 33/33; #8's log record reconciliation-committed first (67a8eab) once green was provable; T1d: ordered-list-sequentiality check added to verify.py — watched FAIL ("line 115: item 3, expected 5"), SKILL.md duplicate item removed, 34/34; whitelist: red-then-green ×2, baseline 33→34, inspector CONFIRMED both; new finding: chained commands (`a && b`) rejected by static validation even when both halves allow-listed (documented alongside T1f); inspector noted check blind spots (indented sub-lists, renumbered duplicates) — accepted scope, revisit under T3d; verify command itself unchanged (lock respected)
#10 | T1e circuit-breaker window | findings(2) | actions(3) | result(green+progress, 37 checks) | diff(~50) | breaker now selects round lines (`^#[0-9]+ \|`) → last 3 → all `result(green+no-progress` (old code tail -n 5'd the whole file — summary tail, not rounds — and any single match bumped an in-memory streak; log-derived state now also survives driver restarts); verify.py +3 checks, both new breaker checks watched FAIL on old code then 37/37 green, plus round-lines↔rounds-done reconciliation (T3d's E4 counter-sum candidate, landed early); whitelist: red-then-green ×2, baseline 34→37 (k=3), inspector CONFIRMED with mutation probing → finding 1: `tail -n 2` mutation passed both checks (check 2 tightened on the spot: `tail -n 3` asserted); finding 2 (pre-existing, unfixed): `claude -p` non-zero exit kills the driver under set -e before the breaker runs; residual: legacy `result(green, …)` rows never count as no-progress (fail-safe direction); verify command unchanged (lock respected)
#11 | T3d rotation (auto-evolve.sh + docs/templates + lessons.md) | findings(4) | actions(3) | result(green+progress, 40 checks) | diff(~85) | findings: (a) #10 residual confirmed — claude -p non-zero exit killed the driver under set -e before any breaker ran; (b) rounds arg unvalidated — bash arithmetic reads '--danger' as 0, so `auto-evolve.sh proj --danger` silently ran zero rounds in danger mode (live bash probe permission-denied for session and inspector alike; settled by shell-semantics analysis on both sides); (c) templates/evolve-log.md round-line example still taught legacy result(green|red,…) — drift from #7's anti-gaming vocabulary; (d) template header and SKILL.md profiling list both omitted the optional push: field. actions: driver — errexit-safe guard around claude -p + abort after 3 consecutive failed sessions (mirrors no-progress breaker) + positive-integer N validation + ${2-5} empty-arg closure + header comment and both READMEs synced; template — anti-gaming vocabulary + push line; verify.py +3 checks. whitelist: red-then-green ×3 (watched FAIL 37/40 → fixed → 40/40), baseline 37→40 (k=3); inspector OVERALL CONFIRMED (incl. set -e/set -u walkthrough, found the empty-arg hole → closed on the spot) + mutation probes → 4 checks tightened (failures=0 must appear twice, negated guard + ${2-5}, ordered grep→tail pipeline, full five-state vocabulary); 2 substring blind spots accepted (-eq-3-in-comment mutant, snapshot-bound rounds-done count) → new T4f; rotation: lessons E1–E5 reviewed current (verified-counter bumps are retrospective-only), report/lessons templates clean; T3d closed; verify command unchanged (lock respected)
#12 | T4f behavioral harness | findings(8) | actions(6) | result(green+progress, 71 checks) | diff(~240) | stop rules extracted to scripts/breaker.sh (single source, driver sources it) + verify.py runs it against 24 fixture logs asserting the stop/continue decision + bash -n ×2 + wiring checks — baseline 40→71. SIX adversarial-inspection rounds: R1 4 surviving mutants (all closed on the spot), R5-6 shadow axis; final replay 15/15 mutants killed, every one by a behavioral check. 4 real bugs fixed red-then-green along the way, each watched FAIL then GREEN: E3 (notes quoting the vocabulary false-stopped healthy runs), F3 (verbatim pipe-quote ditto), G2 (free-form target containing ' | ' silently disabled the breaker), H2/H3 (result-syntax-mimicking target shadowed the real column — incl. deliberate anti-gaming evasion) — breaker now counts the FIRST field with a full 'result(<vocab>, ' shape, scanning from field 3 (awk; FS uses [|] — gawk degrades '\|' to single-space splitting, caught live). Final verdict REFUTED-narrowly: accepted residuals I2 (pipe-shifted full-shape fake segment can still shadow — defense-in-depth margin), I3 (legacy rows + full-shape note segment false-stop), I4 (template-violating no-target line missed, fail-safe), surviving mutants R1/R2/R3/Q1r/Q2r all fix-direction — all require wholesale syntax faking or template violations, recorded in pool note for a future round. CHANGELOG Unreleased backfilled (#9–#12). T4f closed; autonomous-executable pool now empty (T1c is manual-only) → next: retrospective or converged. verify command unchanged (lock respected)
#13 | retrospective (operator-run — driver predates T1g, sessions couldn't know this was the final round) | findings(1) | actions(3) | result(green+no-progress, 71 checks — label corrected in #14: docs-only round, no whitelist credential; work real) | diff(~90) | T1g found and fixed (driver now passes run position + final-round retrospective instruction, c788c52); lessons E1–E4 verified +1 (confirmed by this run), new E6 (permission-layer blockers) + E7 (driver must pass run position); release 1.3.1
#14 | T1g pin + concurrency cleanup (post-#13) | findings(6) | actions(4) | result(green+progress, 74 checks) | diff(~55) | ran CONCURRENTLY with the operator's #13 (one tree, two live round-sessions — mid-round foreign rewrites of lessons.md, foreign commit+push); yielded, waited for #13 to land, re-scoped on top (now codified: lesson E9 + SKILL.md single-writer rule). findings: T1g fix unpinned (no verify check — a behavior flip would pass 71/71); lessons library structurally unguarded; the concurrency hole itself; #13 result overstated (green+progress without a whitelist credential — corrected in place, label not the work); #13 round line sat outside the ## Rounds block (relocated); the run's biggest lesson (substring probes pass behavior-flipping mutants) absent from the library. actions: verify.py +3 — T1g wiring check (watched RED at the abd1647 snapshot as the ONLY failing check of 74, GREEN at HEAD) + lessons template & integer-verified checks, baseline 71→74 (k=3); lessons +E8 (behavioral over substring) +E9 (single writer per tree); SKILL.md autonomous mode amended (driver states run position — E7 now protocol text; single-writer yield procedure); log hygiene (#13 relocated+relabeled, header synced). this session also ran the retrospective's mandatory replay audit (none recorded in #13): #10 and #12 replayed via git-show snapshots — parents green at 34/40 with each round's new checks absent, round commits green at 37/71 — 2/2 clean, no gamed rounds. verify command unchanged (lock respected)
#15 | pool-refresh + README install docs | findings(1) | actions(2) | result(green+progress, 77 checks) | diff(~25) | interactive round (first since #7); refresh triggered by header directive (last refresh #8, full pool sweep since). refresh: T1a mitigation re-verified in both READMEs (stays, environmental), closed entries dropped, Tier 3 re-listed (10 modules, rotated clean by #9–#14; CI workflow reviewed clean) — README install section the last gap. finding: manual-install copies only SKILL.md, omitting docs/templates/ that SKILL.md profiling expects next to it (D7 docs-lag — hit live installing this very skill earlier in the session, evidence: installer had to infer templates from SKILL.md text). fix: both READMEs' install lines + line-anchored verify check (watched RED 74/75 → GREEN 77/77; +2 link-resolution checks from the new README links); inspector CONFIRMED — 4/7 mutants caught incl. the historical shape and both one-sided regressions, fails closed on delete/reword; anchor tightened on the spot to exact `**Manual:**`/`**手动安装：**` prefix after the inspector's first-match-hijack mutant (e) (probe: decoy `**Manual updates:**` line now correctly skipped, mutant caught); accepted residuals: semantic negation + wrong-destination mention — prose wiring-check ceiling, both require rewriting the instruction to be wrong while still mentioning templates. whitelist: red-then-green, baseline 74→77 (k=3); verify command unchanged (lock respected)
#16 | retrospective (interactive run #15–#16, final round) | findings(0) | actions(3) | result(green+no-progress, 77 checks) | diff(~30) | replay audit first: #15 replayed via git-archive snapshots — parent f944462 green 74/74 with the round's new check absent (0 hits), round commit 9168095 green 77/77 with it present — 1/1 clean, no gamed rounds (the run's only whitelist-claiming round; #10/#12 were already replayed in #14). lessons: E2 +1, E8 +1 (both confirmed in action this run), new E10 (installation docs rot until followed verbatim — #15's defect had shipped since #2 and only a live install exposed it); E1/E3–E7/E9 re-checked current, no rewrites. run ended early by design — pool holds no round-sized targets after the refresh, and manufacturing no-progress rounds violates the breaker spirit. summary block appended below (file tail). no SKILL.md revision warranted: this run's only pit (a vacuous file-wide substring check) is already E8's territory. verify command unchanged (lock respected)
#17 | T3 rotation: post-#16 content (diagrams, epics mechanism, role tables) | findings(5) | actions(3) | result(green+progress, 81 checks) | diff(~65) | run of 50 requested; rotated the 5 unreviewed post-#16 operator commits (architecture diagrams, nine-role tables, R1/R3 rules, Epic escalation). findings: F1 both diagrams omitted the epic path / step-5 abandon rule / retrospective epics re-check (D7 drift, two representations disagreeing); F2 epic graduation had no owner in the role tables; F3 epics template content unguarded (existence check only); F4 template placeholder used &lt; entities vs the family's raw <project name>; F5 (inspector, pre-existing) EN diagram step-6 omitted the push exception ZH carries. actions: diagrams completed bilingually (epic branch incl. Tier-4 re-entry, abandon note, retro epics re-check, push exception); Tech Lead + epic graduation calls (3 tables, SKILL+both READMEs); epics-template gate check — watched RED 80/81 on the placeholder conjunct, GREEN 81/81 after the fix, then tightened on the spot with the inspector's two semantic pins (only-by-user-review, one-proposal-per-round — both already present, kills mutants d/f/g). inspector CONFIRMED F1/F2/F4 (3 mutation probes caught incl. truncated status vocabulary); accepted residuals: self-contradictory-template mutant (h) and detail-level diagram omissions (abandon-rule discard clause) — prose wiring ceiling. whitelist: red-then-green, baseline 80→81, inspector-confirmed fixes; verify command unchanged (lock respected)
#18 | T3 rotation: SKILL.md full read-through + driver prompt | findings(7) | actions(4) | result(green+progress, 82 checks) | diff(~60) | findings: F1 Step 0 never read docs/epics.md (the one-open-proposal cap was unenforceable for resuming sessions); F2 "proposals surface in the log header" named no field; F3 driver prompt result vocabulary listed 4 of 5 states (interrupted missing) vs SKILL.md/breaker; F4 a target's pool status after a proposal was undefined — escalation did not stop the grinding; F5–F7 (inspector pass-1, REFUTED the first parking patch): pool refresh re-admitted parked targets (no precedence), all-parked pool deadlocked the driver (blocked rounds don't trip the breaker), header field specified only in first-run profiling. actions: driver prompt 4→5 states (check watched RED 81/82 → GREEN); six spec closures — Step 0 item 4 reads the register; `- epics pending:` wired into template header + step 7 duty + template check (second RED→GREEN); parking gains refresh precedence ("parking outlives refreshes"), fresh-trigger evidence requirement, trigger-(b) disambiguation, template `- target:` field; all-parked → early termination "pending epic decisions" (step 1 + convergence + no manufactured blocked rounds); driver-vocab check re-anchored to the prompt's own span (kills the append evasion E2). inspector pass-2 CONFIRMED all closures across 5 attack surfaces (retrospective-vs-termination compatibility derivable, deleted-module orphans fail safe, E1'/E2 probes caught incl. sixth-state insertions); accepted residuals: E1' full-span comment plant (wiring ceiling, T1g-check class), breaker lacks a pending-epics stop channel → pooled as T1h for #19. whitelist: red-then-green ×2, baseline 81→82, inspector-confirmed (two passes); verify command unchanged (lock respected)
#19 | T1h breaker pending-epics channel + status-line ownership | findings(4) | actions(3) | result(green+progress, 86 checks) | diff(~45) | findings: T1h breaker had no pending-epics stop channel (all-parked autonomous run burns empty sessions to N — #18 pass-2 residual); R1 (inspector, introduced by the fix) the "resumed run resets to active" parenthetical was orphaned — no step owned the reset, capping a post-decision resume at 1 round; R2 (inspector, pre-existing since #10) `- status: converged` equally had no owner instructed to set it; R3 (inspector, pre-existing) Autonomous-mode #4 and both diagrams' breaker boxes listed only no-progress/failed-sessions, omitting the header channels. actions: breaker.sh pending-epics channel (converged's twin) + 4 behavioral fixtures — both positive fixtures watched FAILING on the pre-fix breaker (rc=1), 86/86 after; four-surface sync (SKILL instruction, driver header comment, both READMEs, both diagram termination lines); on-the-spot closures ×4 — step 7 now owns the header status line (active → terminating state → reset on resume, with the stale-header consequence spelled out), convergence bullet instructs setting converged, autonomous #4 lists the header channels, diagram breaker boxes updated. inspector CONFIRMED (probes a–e on copies: dual-channel precedence benign, zero-rounds fires correctly, typo variants fail-closed, red→green independently reconstructed); accepted residuals: whole-file `^- status:` grep can false-stop on a historical col-0 status line inside an old summary block (inherited converged shape), prefix-match stops any `pending-epics-*` status (fail-closed, benign). whitelist: red-then-green ×2, baseline 82→86 (k=4), inspector-confirmed; verify command unchanged (lock respected)
#20 | release v1.4.0 | findings(0) | actions(1) | result(green+no-progress, 86 checks) | diff(~5) | version bump in plugin.json + marketplace.json (1.3.1 → 1.4.0), CHANGELOG Unreleased → [1.4.0] with summary line, tag v1.4.0 created locally (NOT pushed — no-auto-push red line; operator decides). packages: Epic escalation (trigger standard, epics register + template, parking, all-parked termination, pending-epics breaker channel), nine-role senior-team model, bilingual architecture diagrams, token-waste guards (verify-abandon rule, cost-scaling note), #15–#19 fixes; suite 74→86 across the batch. docs+version only — no whitelist credential, recorded no-progress honestly (streak 1/3). verify command unchanged (lock respected)
#21 | breaker status-channel header-region anchor | findings(1) | actions(1) | result(green+progress, 88 checks) | diff(~30) | finding: both status stop-channels grepped the whole file — a col-0 terminating-status line inside an old run-summary block (summary blocks are protocol-encouraged) false-stopped a resumed run (#19 accepted residual; shape inherited from #10's converged channel). fix: channels now read the header region only (sed 1,/^## /p); degenerate logs — no `## ` heading, or a heading AT line 1 (POSIX 1,/re/ never closes; variant found by the inspector, documented on the spot) — fall back to whole-file fail-safe; 2 fixtures watched FAILING pre-fix (historical pending-epics / converged at col-0 → wrongly stopped), 88/88 after. inspector CONFIRMED: CRLF inert (byte-verified on an autocrlf box — col-0 prefix patterns), no-heading degeneration fail-safe, sed-failure guarded fail-open, code-fence truncation fail-open but the channel is cooperative by nature; residuals accepted (degenerate-log whole-file fallback, fail-safe direction). whitelist: red-then-green ×2, baseline 86→88 (k=2), inspector-confirmed; verify command unchanged (lock respected)
#22 | retrospective (interactive run #17–#22, final round — operator ended the run here) | findings(0) | actions(3) | result(green+no-progress, 88 checks) | diff(~30) | replay audit first: #19 and #21 replayed via git-archive snapshots — parents green at 82/82 and 86/86 with each round's mechanism absent (0 grep hits), round commits green at 86/86 and 88/88 with them present — 2/2 clean, no gamed rounds. lessons: new E11 (mechanism-state orphan law — every written state needs set/terminate/reset ownership in a named step; three live cases), E2 → 4 (every whitelist claim this run leaned on the suite), E8 → 3 (two-pass inspection caught a real refutation in #18 and a fix-introduced orphan in #19); E1/E3–E7/E9/E10 re-checked current, no rewrites. crystallization: the verification theme (E2+E8, 7 verifications) already crystallized as the verify suite (#1) and the breaker fixture harness (#12) — no new promotion; no other theme meets the ≥3-lesson bar. process improvements: this run's pits are captured (E11 is the new one; the SKILL.md revisions landed in-round via #18/#19). summary block appended at file tail. run ended by operator instruction after this round. verify command unchanged (lock respected)
#23 | R1 borrow: mid-run lesson capture (cuopt-skill-evolution) | findings(1) | actions(1) | result(green+progress, 90 checks) | diff(~50) | operator directive opened the innovation run (Tier R pool in header): round = research external self-iteration skills → adopt one mechanism. research: VoltAgent/awesome-agent-skills (1000+ curated) → NVIDIA/cuopt skills/cuopt-skill-evolution ("after solving a non-trivial problem, detect generalizable learnings and propose skill updates"). gap: evolve's lessons landed only at the retrospective — a run ended early lost its insights (live: E11 surfaced #18/#19, landed #22). adoption (E5 mechanism-not-architecture): SKILL.md step 7 now captures generalizable lessons in the round's own commit with verified 0 (triggers: thrash before landing, inspector refutation, workaround vs undocumented constraint, protocol gap); capture is recording, not whitelist progress; retrospective re-check gives first verification; lessons.md header documents the convention. whitelist: +2 span-anchored checks watched RED 88/90 → GREEN 90/90, baseline 88→90 (k=2); novelty guard clear (#6 borrowed verified-counters from ECC — different mechanism, different source); verify command unchanged (lock respected)
#24 | R2 borrow: protocol-drift notice (check-update.sh session-start hook) | findings(1) | actions(1) | result(green+progress, 93 checks) | diff(~95) | gap (live): plugin updates land silently — the marketplace auto-moved evolve 1.3.1→1.4.0 mid-day 2026-09-05 with no notice, so a long-lived session (interactive N-round run or auto-evolve driver) keeps executing the stale protocol unknowingly. provenance: this live observation + Claude Code 2026 lifecycle-hook theme (deterministic checks at session/agent key points) + gstack-upgrade self-updater prior art. adoption (E5, notice-never-self-update): scripts/check-update.sh compares the installed manifest against GitHub main (curl -m 5, silent offline — offline must not block or warn; EVOLVE_VERSION_URL override for tests; --compare A B mode exposed as the offline-executable decision core); SKILL.md Step 0 item 5 wires it as a session-start hook: surface one line when newer exists, never self-update, never block the round. whitelist: +3 checks watched RED 90/93 on the script-absent tree, GREEN 93/93 (E8: --compare executed against newer/equal/older fixtures, not substring-probed); live probe: remote equal → silent exit 0 as specified; E11 ownership clear (mechanism writes no persistent state); verify command unchanged (lock respected)
#25 | R3 borrow: seed lessons in the shipped template (Reflexion/Voyager) | findings(1) | actions(1) | result(green+progress, 95 checks) | diff(~35) | operator raised the run to 50 (#23–#72, retro at #72; checkpoint due after #32). research: Voyager (skill library persists only environment-verified skills; starter library), Reflexion (verbal RL; Meta-Policy variant makes reflective memory reusable ACROSS tasks), AlphaEvolve (evaluator strength is the bottleneck) — synthesis: externalized memory + verification-gated entry are the load-bearing mechanisms. gap: new adopters of this skill start with an EMPTY lessons library and re-derive what the evolve project already paid for; 5 of this repo's 11 lessons are fully generic (E2/E5/E8/E10/E11). adoption: docs/templates/lessons.md ships a Seeds block — 5 generic rows under S-ids (never collide with a project's E-numbering), each with executable how-to-apply; SKILL.md profiling tells the copier they exist; lifecycle owned by the existing library-rot rule (first retrospective deletes what the project cannot apply — E11 clean, no new state). whitelist: +2 checks watched RED 93/95 → GREEN 95/95, baseline 93→95 (k=2); novelty guard clear (template work in #2 shipped EMPTY templates — this fills one with vetted content, a different action); verify command unchanged (lock respected)
#26 | R4 borrow: packaging quality lint (VoltAgent standards / eskill validator) | findings(1) | actions(1) | result(green+progress, 97 checks) | diff(~30) | research: community skill-quality standard (VoltAgent/awesome-agent-skills table; eskill's validator concept) — skill metadata is all a picker sees, bodies must stay lean, shipped artifacts must be portable. gap: evolve's own suite lints structure (frontmatter, links, wiring) but nothing guards packaging quality — a future round could bloat SKILL.md past usability or leave a machine-specific path in a shipped file without a single check failing. adoption (evaluator self-strengthening, AlphaEvolve's core lesson applied at home): verify.py +2 — SKILL.md body <500 lines (now 168, grows guarded) and no machine-specific absolute paths (regex [A-Za-z]:[\\/] | /Users/) across SKILL.md + all shipped templates. whitelist: baseline 95→97 (k=2) all green, PLUS mutation probes proving the guards bite: planted `C:\Users\fake` in a template → FAIL naming lessons.md; bloated SKILL.md to 645 lines → FAIL with count; both reverted, 97/97 restored (E8 spirit — guards demonstrated against live mutations, not assumed); novelty guard clear (no prior lint round); verify command unchanged (lock respected)
#27 | R3 borrow: mutation self-audit in the replay audit (AlphaEvolve) | findings(2) | actions(2) | result(green+progress, 98 checks) | diff(~40) | research: AlphaEvolve's generate–evaluate–select loop — "weak/no evaluators are the main bottleneck"; row count ≠ detection power. gap: the retrospective's replay audit replays commits but never verifies the run's NEW checks actually detect anything — a suite could grow +20 vacuous checks and pass every audit. adoption: anti-gaming rule 5 extended — the audit also mutation-probes 2 of the run's newly added checks (flip/garble the guarded artifact, expect FAIL, revert); probe outcomes go in the retrospective log line; +1 span check watched RED 97/98 → GREEN 98/98. FIRST SELF-AUDIT EXECUTED (evidence): probe 1 delete S5 seed row → seed-count check FAIL "4 seed rows" ✓; probe 2 garble never→rarely in Step 0 → drift-notice check FAIL ✓; both targeted-reverted, 98/98 restored. incident (found + fixed in-round): first probe attempt used S3→S9 rename (no contract flip — my probe was wrong, check's count contract held) and reverting via `git checkout --` wiped the uncommitted rule-5 text from SKILL.md — caught by the post-revert verify run (98→97), work re-landed; captured mid-run as E12 per the #23 mechanism (its first live trigger): revert probes with targeted inverse edits, never `checkout --` a file with uncommitted round work. whitelist: baseline 97→98 (k=1, non-consecutive trivial — #26 was k=2) + red-then-green on the span check; novelty guard clear (audit extension never touched since #5/#14); verify command unchanged (lock respected)
#28 | R2 borrow: round-complete hook in the driver (Claude Code lifecycle hooks) | findings(1) | actions(1) | result(green+progress, 101 checks) | diff(~40) | research: Claude Code 2026 changelog — deterministic lifecycle hooks at key agent moments (post-edit, pre-command, model-switch gates) are the control surface of choice. gap: the driver had NO extension point — everything after a round is hardwired, so an operator wanting a notification/deploy/metrics-export per round had to fork the script. adoption: AUTO_EVOLVE_ROUND_HOOK env var — after every successful session the driver runs the command (sh -c, advisory: failure reported, run continues; failed sessions fire nothing; E11 stateless); driver header documents it, both READMEs document it (E10: feature and docs land in the same round, not "at release"). whitelist: +3 span-anchored checks watched RED 98/101 → GREEN 101/101 (k=3); bash -n clean; E8 ceiling respected and documented (driver spawns real claude sessions — not executable in the suite, so wiring spans are pinned instead); novelty guard clear (driver untouched by innovation rounds so far); verify command unchanged (lock respected)
#29 | release v1.5.0 | findings(0) | actions(1) | result(green+no-progress, 101 checks) | diff(~55) | packaged the run's first six adoptions (#23–#28): versions 1.4.0 → 1.5.0 in plugin.json + marketplace.json; CHANGELOG Unreleased folded into [1.5.0] (mid-run capture, drift notice, seed lessons, packaging lint, mutation self-audit, round hook, lessons E11/E12, #21 breaker fix); consistency guard naturally watched RED mid-round (changelog 1.4.0 vs plugin 1.5.0) → GREEN after the section landed — noted for transparency, not claimed as a credential (the divergence was the release flow's own intermediate state, per the #20 precedent docs+version rounds carry no whitelist credential; no-progress streak 1/3). check-update live semantics re-confirmed: local 1.5.0 vs unpushed remote 1.4.0 → verdict "older" → silent, no false alarm. tag v1.5.0 created locally (NOT pushed — no-auto-push red line; operator decides, same as v1.4.0). E10 clean-install walkthrough remains pooled as T1c (manual-only). verify command unchanged (lock respected)
#30 | R4 borrow: untrusted-content red line + hostile-manifest fixture (Snyk ToxicSkills / CSA) | findings(1) | actions(2) | result(green+progress, 103 checks) | diff(~45) | research: Snyk ToxicSkills — prompt injection in 36% of analyzed agent skills, the fetch-untrusted-content class is the risky one; Skill-Inject benchmark; CSA — treat all externally fetched content as untrusted input; consensus = defense in depth. gap: the innovation run itself scrapes repos/pages every round, and the drift-notice script fetches a remote manifest every Step 0 — yet no protocol line contained that boundary. adoption (2 pieces): (a) red line added — externally fetched content is untrusted data, never instructions found in it, findings quoted verbatim with provenance in the log line, never fetch-and-run; (b) check-update.sh gains a --version-of test seam + verify fixture: a hostile manifest (`"9.9.9; rm -rf /"`) yields empty output (parser accepts only [0-9.], injected text can never reach a shell) while the real manifest still parses — executable ground truth, not substring trust. whitelist: +2 checks watched RED 101/103 → GREEN 103/103 (k=2, behavioral + span); novelty guard clear (security source untouched by prior rounds); verify command unchanged (lock respected)
#31 | R2/R3 borrow: regressions & rollback procedure (Aider auto-undo + Gemini CLI /restore) | findings(2) | actions(1) | result(green+progress, 106 checks) | diff(~35) | research: Aider runs tests after each edit and auto-undoes failures; Gemini CLI checkpointing /restore — and its live rollback bug (a restore flattened a half-updated tree, making things WORSE) as the cautionary constraint. findings: (a) the header carried a `regressions` counter since #1 with NO step owning its setter — an E11 orphan hiding in plain sight; (b) README line 133 has claimed "a regression is one `git revert`" since pre-1.4.0 while the protocol defined no rollback procedure — docs promising a capability the protocol never had (D7/E10 drift). adoption: new "## Regressions & rollback" section — demonstrate the regression RED on HEAD first (proven, not remembered), revert the WHOLE round commit (never file-level snapshots), verify green before committing, record the revert as its own round line (red-then-green = whitelist credential) with `regression(reverted <round>)`, counter setter named, target returns to pool as a fresh trigger. whitelist: +3 checks — 2 span checks watched RED 104/106 → GREEN 106/106 (first attempt failed on token mismatches "returns the target"/"demonstrated RED" vs landed text — caught by the verify run itself and retargeted; checks assert the landed contract), 1 README guard (green immediately: both READMEs' pre-existing `git revert` mentions now guarded against removal); novelty guard clear (no prior round touched rollback); verify command unchanged (lock respected)
#32 | R2 borrow: driver --dry-run (Claude Code plan mode / Gemini CLI preview) + checkpoint | findings(1) | actions(1) | result(green+progress, 110 checks) | diff(~55) | research: Claude Code's plan mode and Gemini CLI's preview idea — show what WOULD run before anything runs; operator cost guard (README already said "pilot with small N" but the only free pilot was reading the script). gap: the driver's only validation mode was launching real paid sessions. adoption: leading `--dry-run` flag — validates args/rounds/log presence, then prints the plan (project, rounds, perms, log pointer, breaker verdict via the real breaker, hook) and exits 0 spawning nothing. whitelist: +4 checks — dry-run behavioral (E8-executable: the dry path spawns no sessions, so the driver RUNS in the suite: output asserted, exit 0), argument-validation pin (watched the non-numeric rejection survive the new flag path — #11's fix guarded), header + bilingual README wiring; the 3 new-content checks watched RED 107/110 → GREEN 110/110; live probe: `--dry-run . 3` printed pointer=#32, breaker continue, hook none. 10-ROUND CHECKPOINT written to header (rounds #23–#32, baseline 88→110, v1.5.0 local, pool/sources snapshot, next sources listed). novelty guard clear; verify command unchanged (lock respected)
#33 | review-fix round: records hygiene (operator-ordered review of the run) | findings(3) | actions(3) | result(green+progress, 113 checks) | diff(~45) | operator ordered a review of the iterations (main content by the session, visuals by a read-only subagent); this round lands the records-hygiene findings. F1 E4 VIOLATION found live: header findings 63 vs Σ round lines 64, off by one at #27 (2-findings round, +1 bump) — and the suite only guarded row-count↔rounds-done, never the sum; fixed header 63→64 AND added the sum check, which was watched FAILING on the real violation (detail line: "sum 64 of 32/32 rows vs header 63") → GREEN — textbook red-then-green on a genuine defect. F2 `fixes` counter had no defined mapping to round-line fields (unguardable by construction; frozen at 49 while 12 actions landed) — semantics now written into step 7 (fixes = actions that fixed confirmed defects, subset of actions) + inequality guard fixes ≤ Σ actions (landed green: 49 ≤ 69). F3 dead variable `shipped` in verify.py's packaging lint (computed, never used) — removed. whitelist: baseline 110→113 (k=3), one check red-then-green on a real violation; novelty guard clear (no prior round guarded the sum); verify command unchanged (lock respected). Review findings F4–F9 land in #34–#35
#34 | review-fix round: portability & fetch hygiene | findings(3) | actions(3) | result(green+progress, 118 checks) | diff(~55) | F4 no .gitattributes on an autocrlf=true box — a fresh Windows clone materializes CRLF .sh files and bash chokes on $'\r' (the commit-time "LF will be replaced by CRLF" warnings were the live symptom); fixed: .gitattributes pins `*.sh text eol=lf` (+ check RED→GREEN). F5 check-update.sh fetched the remote manifest on EVERY Step 0 with no cap — a 50-round driver run pays 50 fetches, worst case 5s each (the 24h-cache constraint was discussed at design time but never landed); fixed: successful fetches seed a 24h cache (EVOLVE_CACHE_FILE override for tests; cache state is script-private, staleness fails silent — E11-clean; note: a fresh cache delays NEW remote versions by ≤24h, by design) + behavioral check RED→GREEN (seeded fresh cache "9.9.9" + unreachable URL → notice served from cache, network never touched). F6 manual-install line covered SKILL.md + docs/templates/ but NOT scripts/ — drift notice silently absent for manual installs (D7-class); fixed in both READMEs (+ conjunct on the anchored install check RED→GREEN; +2 link-resolution checks from the new links). whitelist: baseline 113→118 (k=5: gitattributes, cache behavioral, install conjunct, 2 link checks), 3 watched RED→GREEN; regression probes re-run (--compare newer ✓, hostile-manifest inert ✓); verify command unchanged (lock respected)
#35 | review-fix round: rollback completeness + visual parity | findings(3) | actions(3) | result(green+progress, 120 checks) | diff(~40) | F7 the #31 rollback procedure broke in practice: `git revert` of a non-latest round commit conflicts inside docs/evolve-log.md (later round lines interleave) and the procedure had no resolution rule — "reverts as a unit" was unworkable for mid-run rounds; fixed: step 2 now owns the conflict rule — keep the log fully intact, the revert removes the round's effect on code and shipped docs, never the record; re-add deleted lines, keep later rounds' lines (+ span check RED→GREEN). F8 diagram structural defects (subagent-confirmed): ZH step-1 description one column left of every other row (19 vs 20 — fixed, one space); floating `──►` under the epic branch connected to no trunk (fixed in both files: `│` trunk at col 7, arrow unchanged at col 26); ragged wrap on the loop-back label (EN to col 30 under the parenthesis content, ZH to col 25). F9 bilingual drift: EN "hand off to a fresh session" (directive) vs ZH 建议 (suggestion, matching SKILL.md) — EN reworded to "suggest a fresh session resuming from the pointer" (+ guard: suggest-language present, hand-off absent — RED→GREEN); two ZH-only details ported to EN (pool refresh "part of that round's work, no separate commit"; verify lock "own commit to change"). Judgment call recorded: the two textual loop-back labels ("back to 1", "slices re-enter Tier 4") stay as the consistent bilingual idiom — a physically drawn return edge would cross the whole loop block; the reviewer's own fallback option. Lesson E13 captured mid-run per the #23 mechanism (delegated visual review returned 1 false positive in 8 — "(a–f) dangling" refuted by SKILL.md's trigger standard; cross-check before acting). whitelist: baseline 118→120 (k=2), both watched RED→GREEN; novelty guard clear; verify command unchanged (lock respected)
#36 | user-directive feature: time-budgeted iteration (--until / --for) | findings(2) | actions(3) | result(green+progress, 127 checks) | diff(~150) | operator asked for deadline/duration iteration ("迭代到20260907 09点" / "迭代5小时"). research: industry consensus — budgets checked at the TOP of each loop iteration (Strands Agents loop docs), layered limits (max iterations + wall-clock, buildmvpfast guide), graceful termination over hard kills (timeout-aware agent design; deepagents #4670 "a stuck agent burns time until something external kills it") — a hard `timeout` wrapper was the rejected alternative: it kills sessions mid-round and loses uncommitted work. adoption: (a) driver `--until <datetime>` (GNU date, future-validated) / `--for <duration>` (5h/90m, unit required) — mutually exclusive with a count, cooperative gate (launch rounds only while remaining > reserve, AUTO_EVOLVE_MIN_RESERVE default 900s; AUTO_EVOLVE_MAX_ROUNDS cap 0=unlimited), position prompt states deadline+remaining per session, and the driver itself launches the ONE final retrospective session when the budget is reached (T1g: never depend on a session guessing the clock); (b) SKILL.md — Input defines time budgets (gates round starts, never kills a session mid-round, both limits may combine, checkpoints carry the deadline — no new persistent state, E11), step 1 owns the clock check, convergence gains the budget-exhausted termination, autonomous mode #1 documents the flags; (c) both READMEs gain usage + semantics (bilingual), dry-run prints the budget plan. BUG found live by manual dry-run probe and captured as E14: a second `=~` in the same condition cleared BASH_REMATCH → `--for 2h` parsed as 0h ("~0 min remain") — existence-only assertions on the output did NOT catch it; fixed (groups captured immediately, glob companion check) and the dry-run check tightened to pin the magnitude ("~0 min remain" must not appear for a 2h budget) — watched RED on the buggy tree → GREEN. two EXISTING driver checks broke on refactor literals ($i→$launched, N=${2-5} gone) — guards doing their job; anchors re-pointed at the equivalent new literals, intent unchanged. whitelist: baseline 120→127 (k=7), behavioral dry-run checks (time plan RED→GREEN, rejections landed-green), live probes: --for 2h → "~119 min remain", --for 90m → "~89", --until past/bad/mixed → rc=1 ×3, rounds mode unchanged; novelty guard clear (no prior time-termination anywhere); verify command unchanged (lock respected)

## Run summary (2026-09-05, 5 rounds)

- Outcome: baseline 0 → 32 automated checks; CI live; templates shipped; v1.2.0 released; 4 lessons seeded, 2 amended back into SKILL.md
- Closed: T1b, T2, T3b, T3c, T4a, T4b · Clean: T3a · Mitigated: T1a (documented, environmental)
- Open: T1c install path untested in a clean environment (needs a manual `/plugin marketplace add` test) · Backlog: trigger-match eval set, ≥20-round long-run validation of the v1.1/v1.2 mechanisms
- Lesson index: E1 name collision, E2 docs-repo verification, E3 autonomous verify command, E4 counter discipline

## Autonomous run summary (#8–#12, 2026-09-05, driver auto-evolve.sh, 5 headless sessions)

- Outcome: 1 blocked round (honest refusal to fake progress under a permission wall) + 4 progressive rounds; verify baseline 33 → 71 checks; 4 real parser bugs + 3 driver bugs fixed, all red-then-green; adversarial inspectors confirmed every round and killed 15/15 mutation probes in #12
- Anti-gaming held under fire: whitelist credentials in every log line; verification lock respected 5/5 rounds; #12 even defended against deliberate result-syntax-mimicking evasion; the one unrunnable round recorded `blocked` instead of narrative progress
- The one manual action the system asked for: adding PowerShell(...) permission rules (T1f) — 30 seconds of operator time unblocked the remaining 4 rounds
- Gap found by the run itself: no session knew it was the final round → retrospective skipped → T1g (fixed post-run, E7)
- Closed: T1d, T1e, T1f, T3d, T4f, T1g(post) · Open: T1c (manual clean-env install test), I2–I4 breaker residuals (recorded in #12), big-ticket backlog (trigger eval set, ≥20-round external long-run, graph-ready tagging per the 2026-09-05 evaluation)

## Operator note (post-#14, root cause of the phantom session and the driver's exit 2)

The #14 session correctly recorded the collision but could not see its root cause: the operator (this session) edited `scripts/auto-evolve.sh` in place while the driver was still executing round 5 — bash re-reads scripts incrementally, so the edit shifted byte offsets, produced the `line 80: unexpected EOF` exit 2, and re-executed the loop body with the new FINAL-round prompt, launching the concurrent "phantom" session. Recorded in E9's how-to-apply: driver edits only between runs.

## Interactive run summary (#15–#16, 2026-09-05, single session)

- Outcome: pool refreshed (first since #8) — Tier 3 fully rotated, closed entries pruned; 1 real docs defect found & fixed red-then-green (manual-install template omission, hit live during this session's own install); verify 74 → 77; adversarial inspector CONFIRMED with on-the-spot anchor tightening; replay audit 1/1 clean
- Ended early by design: T1c is manual-only, Tier 4 is big-ticket (spec→plan), I2–I4 deferred — no round-sized targets, and no-progress padding is forbidden
- Open for the operator: (1) T1c clean-env plugin install test — `/plugin marketplace add tangjianfang/evolve` + `/plugin install evolve@evolve-marketplace` in a clean environment (not on a machine with an existing manual install), (2) commission the trigger-match eval set or the ≥20-round external long-run as a spec, (3) declare converged for now
- Lesson index: E10 added; E2, E8 verified +1

## Interactive run summary (#17–#22, 2026-09-05, single session, N=50 requested)

- Executed 6 of 50 rounds; ended by operator instruction after the retrospective — the pool held no round-sized targets (T1a environmental, T1c manual-only, Tier 4 big-ticket, I2–I4 deferred), and burning the remaining 44 as no-progress rounds would only have tripped the breaker
- Outcome: post-#16 operator changes rotated clean (#17, 5 findings, bilingual diagram + role-table drift); Epic-mechanism spec gaps closed through a two-pass inspection (#18 — pass-1 REFUTED the first parking patch, pass-2 confirmed the closures); T1h breaker pending-epics channel + header-status ownership (#19); v1.4.0 released (#20, tag local); breaker status channels header-anchored (#21); suite 80 → 88 checks
- Four adversarial inspections, all CONFIRMED; two produced on-the-spot closures (anchor tightening, status-ownership fixes), one absorbed a real refutation (#18 pass-1); replay audit 2/2 clean (#19, #21)
- Lessons: E11 added (mechanism-state orphan law), E2 → 4, E8 → 3; verification theme already crystallized (verify suite #1, breaker harness #12)
- Open for the operator: v1.4.0 tag + 6 local commits unpushed; T1c clean-env install test; Tier 4 commissions (trigger-match eval set, ≥20-round external long-run); I2–I4 stay deferred
